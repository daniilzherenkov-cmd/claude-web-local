[project]
name = "claude-web-local"
version = "2.0.0"
description = "Browser UI for Claude Code via the Claude Agent SDK"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk>=0.2.82",
    "aiohttp>=3.9.0",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.1.0",
]
